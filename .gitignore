__pycache__/
*.pyc
*.egg-info/
dist/
build/
.venv/
.eggs/
*.so

# Pennyfarthing / agent session state — should only live in the orchestrator,
# but tools launched from the wrong CWD can leak .session/ into here.
.session/

# Git worktrees — local parallel-plan workspaces, never commit.
.worktrees/
